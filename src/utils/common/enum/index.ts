export enum SYS_ROLE{
    user,
    admin,
    superAdimn
}

export enum GENDER {
    male,
    female
}

export enum USER_AGENT {
    local,
    google
}

export enum REACTION {
    like,
    love,
    care,
    wow,
    sad,
    angry
}

export enum FRIENDREQUESTSTATUS {
    pending,
    accepted,
    rejected
}